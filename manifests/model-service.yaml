apiVersion: v1
kind: Service
metadata:
  name: model-service
  labels:
    app: model-serving
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5001
    nodePort: 30501
    name: http
  selector:
    app: model-serving
